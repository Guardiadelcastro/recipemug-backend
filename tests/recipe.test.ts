import {toModel} from '../src/helpers/DTORecipeHelper';
import { ModelIRecipe } from '../src/models/recipe';
describe('First tests', () => {
  test('should to be 4', () => {
      expect(2+2).toEqual(4);
  });
})



